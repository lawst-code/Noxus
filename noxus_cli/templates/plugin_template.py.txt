from domain.plugins import Plugin
from domain.nodes import Node
from typing import Dict


class {plugin_class_name}Plugin(Plugin):
    title = "{plugin_title} Plugin"

    def nodes(self):
        return [{plugin_class_name}Node()]


class {plugin_class_name}Node(Node):
    name = "{plugin_name_lower}-node"
    title = "{plugin_title} Node"
    description = "A node for {plugin_name}"

    def call(self, input_text: str) -> Dict:
        # Plugin logic here
        return {{"result": input_text}}
